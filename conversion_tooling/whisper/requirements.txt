# Whisper ONNX Conversion Tooling Dependencies
#
# IMPORTANT: These versions are pinned to match the known working experiment setup.
# transformers 4.57+ has a regression affecting Whisper encoder-only training.
# See: backend/training/experiments/training_performance_investigation.md

# Core dependencies - use oldest available versions
# Note: torch 2.5.1 is no longer on PyPI, 2.6.0 is oldest available
torch==2.6.0
torchaudio==2.6.0

# HuggingFace ecosystem - pinned to known well working version aligned with training environment
transformers==4.46.3
optimum[onnxruntime]>=1.19.0,<2.2.0

# ONNX tooling
onnx>=1.16.0,<1.21.0
onnxscript>=0.5.0,<0.7.0
onnxruntime>=1.19.0
