# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/

# Source models (development only, NOT bundled with app)
# Use conversion_tooling/build_assets.sh to generate bundled assets from models/
/models/

# Generated model assets (large files, generated via build_assets.sh)
assets/transcribers/whisper/models/

# Exclude ONNX files EXCEPT in assets/ (we want to commit bundled assets)
*.onnx
!assets/**/*.onnx

# transcriber assets generated via conversion scripts
assets/transcribers

# Python conversion tooling
conversion_tooling/**/venv/
conversion_tooling/**/venv_*/
**/__pycache__/

# Native libraries for unit testing (see NATIVE_LIB_SETUP.md)
*.dylib
*.so
*.dll

notes
